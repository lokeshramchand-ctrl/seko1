import 'package:flutter/material.dart';

class Greenies extends StatelessWidget {
  const Greenies({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
